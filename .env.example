# Copy this file to .env and fill in your Google OAuth app credentials.
# Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# Optional for PKCE/native style clients, but supported if your OAuth client requires it.
GOOGLE_CLIENT_SECRET=
