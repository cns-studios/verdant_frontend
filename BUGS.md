* Settings. "Check for Updates" button should transform to the Download & Install Update button, instead of having them both. and it should download and install, not just download.
* Active Formatting Tools in compose window should be highlighted, so the user knows what is formatted on the text
* The "more" button is currently bugged, doing nothing when clicking.
* Deep Search on IMAP Accounts isnt working (afaik)
* IMAP Accounts dont have the "loading more mails" thingy
* IMAP accounts should use the IMAP feature to receive new mails into their inbox faster, instead of relying on polling every 45 secs
* Things like read, archived, deleted, ect. emails should be synced to provider. both gmail and IMAP/SMTP
* IMAP accounts emails feel "shuffled" in the inbox, not sorted by date.
* App decorations are missing in the onboarding screen (horrible UX)